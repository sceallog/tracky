<div class="container">
  
</div>

<div class="container">
  <div class="card">
    <div class="card-header d-flex">
      <h4 class="card-title mt-auto mb-auto">
        Add a new Project
      </h4>
    </div>
    <div class="card-body">
      <%= form_with(model: @project, local: true) do |f| %>

        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control mb-3' %>

        <%= f.label :start_date %>
        <%= f.date_field :start_date, class: 'form-control mb-3' %>

        <%= f.label :target_end_date %>
        <%= f.date_field :target_end_date, class: 'form-control' %>

        <div class="form-group mt-2 d-flex justify-content-center">
          <%= button_tag(class: 'btn btn-circle ok-btn btn-lg') do %>
                  <i class="fas fa-check"></i>
          <% end %>

          <%= link_to projects_path, class: 'btn btn-circle delete-btn btn-lg' do %>
            <i class="fas fa-times"></i>
          <% end %>
        </div>

      <% end %>
    </div>
  </div>
</div>